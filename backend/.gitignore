# Artefacts de build Rust
/target

# Secrets : ne JAMAIS committer de fichier .env réel (cf. exigence sécurité #8)
.env
.env.*
!.env.example

# Certificats OpenSearch générés localement (PKI de dev) : JAMAIS committés.
/docker/opensearch/certs/

# Divers
*.log

# Fichier de persistance Redis créé par un redis-server lancé en local pour les
# tests (écrit dans le CWD) : artefact de dev, jamais committé.
dump.rdb
